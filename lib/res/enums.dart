enum FoodType { FASTFOOD, CONTINENTAL, ITALIAN, THAI }

enum NotificationType { SIMPLE, RATED }

enum selectYourStatus { unselect, select }

enum MessageType { SEND, RECEIVED }

enum OrderStatus {
  pending,
  acceptedByRestaurant,
  rejectedByRestaurant,
  rejectedByAdmin,
  riderAssigned,
  rideStarted,
  rideCompleted
}
